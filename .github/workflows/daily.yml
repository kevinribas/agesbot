name: Enviar Mensagem no Discord

on:
  schedule:
    - cron: "0 22 * * 1,3,4,5,6"
  workflow_dispatch:

jobs:
  send_message:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar mensagem para o Discord
        run: |
          curl -H "Content-Type: application/json" \
               -X POST \
               -d '{"content": "**🚀 HORA DA DAILY!**\n\n**📝 Exemplo:**\n📌 *O que fiz ontem:* ...\n🎯 *O que farei hoje:* ...\n⚠️ *Impedimentos:* ..."}' \
               ${{ secrets.DISCORD_WEBHOOK }}
